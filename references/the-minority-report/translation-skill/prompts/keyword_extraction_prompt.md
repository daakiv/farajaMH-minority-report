You are a domain expert. Analyze the following text and extract at least 10 (or more if possible) critical domain-specific concepts or phrases that are directly related to the target term: "{{term}}".
IMPORTANT: 
- Extract DISTINCT, non-redundant core phrases. 
- Do NOT list slight variations of the same concept (e.g., if you extract "water droplets", do NOT also extract "microscopic water droplets" and "suspension of water droplets"). Focus on the most frequent, base combinations of words.
- Each extracted concept MUST be a highly specific multi-word phrase (at least 2-3 words long) that provides specialized context for {{term}}. 
- Do NOT extract generic words (like "Definition", "Standard"). Focus on compound technical terms.
Output ONLY the concepts as a comma-separated list, with no additional text or formatting.

Target Term: {{term}}

Text:
{{text}}
